# Surrogate trajectories

Store reduced-model outputs here. Do not put these files in the EMT reference
folder even when an older filename starts with `ParaEMTDF`. Record the actual
backend, noise structure, alpha, sigma, RNG seed, output interval, internal step
where applicable, input checksums, and solver version. These outputs cannot replace
EMT targets in the calibration archive.
